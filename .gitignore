# Segredos
.env
.env.local
*.env.local

# Estado do bot (fica no volume, nunca no Git)
seen_ids.json
send_queue.json
daily_stats.json
quota_log.jsonl
skipped_jobs.jsonl
bot_state.json
publicadas.json
chat_id.json
bot.log
bot/data/

# SESSÃO DO FACEBOOK. Vale tanto quanto a senha da conta: quem tiver este
# arquivo entra na conta sem login. Nunca commitar, nunca mandar por chat.
fb_state.json
*fb_state*.json

# Cópia que o listar_grupos.py deixa antes de reescrever a lista de grupos.
*.bak

# Python
__pycache__/
*.pyc
.venv/

# Node / Next.js (painel)
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Documentação de trabalho — contexto interno, não vai pro repositório
docs/

# Identidade visual: 4,4 MB que o Coolify clonaria a cada deploy sem usar
brand/
